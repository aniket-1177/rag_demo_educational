# Configuration file for the project
import os

# LLM settings (5.2: Token limitations - Groq models have context limits, e.g., 8k tokens)
GROQ_API_KEY = 
LLM_MODEL =   # Groq model with

# Embedding model (5.3: Vector embeddings)
EMBEDDING_MODEL =   # HuggingFace sentence-transformers

# Vector DB (5.4: Chroma for local demo; Pinecone for cloud)
VECTOR_DB_PATH =   # Local persistence for Chroma

# Chunking (5.5)
CHUNK_SIZE =   # Characters per chunk
CHUNK_OVERLAP = 

# Retrieval (5.6, 5.7, 5.8)
TOP_K = 
RERANK_MODEL =  # For re-ranking